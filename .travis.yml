language: java
sudo: false
cache:
  directories:
    - $HOME/.m2
script: mvn clean install --quiet -B
jdk:
    - oraclejdk8
branches:
    only:
        - master
notifications:
    email:
        - devacfr@mac.com
